# GCS
GCS_BUCKET=cbb-tracking
GCS_PREFIX=video/video-annotation-tasks

# Server
PORT=3001
SESSION_SECRET=change-me-to-a-random-string-in-production
NODE_ENV=development

# CORS — must match frontend URL
FRONTEND_URL=http://localhost:5173
